# Reproduced outputs

The reproduction scripts write newly generated CSV files to this directory.
Generated CSVs are ignored by Git so that a reviewer cannot accidentally
overwrite or commit changes to the manuscript's reference outputs.

Compare new files here with the corresponding files under
`../reference/`. Runtime columns and time-limited MILP details can vary by
machine and solver version.
